name: radxa-cm3-io
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
steps:
  - env:
      GOPATH: /tmp/go
    network: default
    cachePaths:
      - /.cache/go-build
      - /tmp/go/pkg
    prepare:
      - |
        cd /pkg/src
        go mod download
  - env:
      GOPATH: /tmp/go
    cachePaths:
      - /.cache/go-build
      - /tmp/go/pkg
    build:
      - |
        cd /pkg/src
        CGO_ENABLED=0 go build -o ./radxa-cm3-io .
    install:
      - |
        mkdir -p /rootfs/installers/

        cp /pkg/src/radxa-cm3-io /rootfs/installers/radxa-cm3-io
finalize:
  - from: /rootfs
    to: /rootfs
